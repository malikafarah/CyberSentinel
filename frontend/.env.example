# Leave this blank in dev — the Vite proxy handles /api calls locally.
# In production on Vercel, set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://cybersentinel-backend.vercel.app
VITE_API_URL=
